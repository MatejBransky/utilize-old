{
  "$schema": "https://json.schemastore.org/lerna",
  "packages": ["packages/*"],
  "npmClient": "yarn",
  "useWorkspaces": true,
  "version": "independent",
  "command": {
    "publish": {
      "registry": "https://registry.npmjs.org"
    },
    "version": {
      "allowBranch": "main",
      "ignoreChanges": ["**/*.md"]
    }
  }
}
